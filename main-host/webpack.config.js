const HtmlWebPackPlugin = require("html-webpack-plugin");
const ModuleFederationPlugin = require("webpack/lib/container/ModuleFederationPlugin");
const deps = require("./package.json").dependencies;

const REMOTE_ENTRIES_PATH = {
  header: "http://localhost:5002/assets/remoteEntry.js",
  content: "http://localhost:5003/assets/remoteEntry.js",
  footer: "http://localhost:5004/assets/remoteEntry.js",
  store: "http://localhost:5005/assets/remoteEntry.js",
};

module.exports = {
  output: {
    publicPath: "http://localhost:8080/",
  },

  resolve: {
    extensions: [".tsx", ".ts", ".jsx", ".js", ".json"],
  },

  devServer: {
    port: 8080,
    historyApiFallback: true,
  },

  module: {
    rules: [
      {
        test: /\.m?js/,
        type: "javascript/auto",
        resolve: {
          fullySpecified: false,
        },
      },
      {
        test: /\.(css|s[ac]ss)$/i,
        use: ["style-loader", "css-loader", "postcss-loader"],
      },
      {
        test: /\.(ts|tsx|js|jsx)$/,
        exclude: /node_modules/,
        use: {
          loader: "babel-loader",
        },
      },
    ],
  },

  target: "es2020",
  experiments: {
    outputModule: true,
  },

  plugins: [
    new ModuleFederationPlugin({
      name: "main-host",
      library: { type: "module" },
      filename: "remoteEntry.js",
      remotes: {
        HeaderModule: REMOTE_ENTRIES_PATH.header,
        FooterModule: REMOTE_ENTRIES_PATH.footer,
        ContentModule: REMOTE_ENTRIES_PATH.content,
      },
      exposes: {},
      shared: {
        ...deps,
        react: {
          singleton: true,
          requiredVersion: deps.react,
        },
        "react-dom": {
          singleton: true,
          requiredVersion: deps["react-dom"],
        },
      },
    }),
    new HtmlWebPackPlugin({
      template: "./index.ejs",
      inject: false,
    }),
  ],
};
